version: "3.9"

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: recommendation_app
    ports:
      - "5000:5000"
    volumes:
      - .:/app
    environment:
      - FLASK_APP=app.py
      - FLASK_ENV=production
